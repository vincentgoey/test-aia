import React from 'react';
import {StyleSheet} from 'react-native';

export const color = {
  themeButton: '#ff0300',
  black: '#000000',
  white: '#ffffff',
};

export const themeView = StyleSheet.create({
  loadingView: {position: 'absolute', top: 0, left: 0, bottom: 0, right: 0},
});
